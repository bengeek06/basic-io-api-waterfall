"""Integration tests for basic_io_service.

Integration tests test the interaction between multiple components
using lightweight mock services instead of real external dependencies.
These tests validate complete workflows (e.g., export → import).
"""
